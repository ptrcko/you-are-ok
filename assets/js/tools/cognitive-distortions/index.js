// Entry point for the Cognitive Distortions tool. Mirrors the tool module pattern
// so additional tools can reuse the same structure without global changes.
import { initCognitiveDistortions } from './controller.js';

initCognitiveDistortions();
